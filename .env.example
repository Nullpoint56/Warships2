# Debug switches
# 1 = enabled, 0 = disabled
WARSHIPS_DEBUG_INPUT=1
WARSHIPS_DEBUG_UI=1

# Logging
# LOG_FORMAT: json|text
# LOG_LEVEL: DEBUG|INFO|WARNING|ERROR
LOG_FORMAT=json
LOG_LEVEL=DEBUG

# UI viewport scaling
# contain = preserve 1200x720 aspect (recommended), stretch = fill full window with non-uniform scaling
ENGINE_UI_ASPECT_MODE=contain

# Window mode at launch (GLFW backend)
# windowed = normal resizable window
# maximized = maximized window
# fullscreen = fullscreen window
ENGINE_WINDOW_MODE=windowed
