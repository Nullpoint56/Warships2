[project]
name = "warships2"
version = "0.1.0"
description = "Classic Battleship (Warships) built with Python and PyQt6."
requires-python = ">=3.14,<3.15"
dependencies = [
    "numpy>=2.0",
    "PyQt6>=6.7",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
]
