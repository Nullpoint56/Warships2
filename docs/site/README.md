# Documentation Site Artifacts

- `build/` contains generated Sphinx output.
- `source/` contains Sphinx source configuration.

These files are generated/maintained separately from the curated architecture/plan/history docs in `docs/`.
