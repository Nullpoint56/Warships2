# Architecture Docs

- `system_overview.md`: high-level engine + game architecture.
- `import_boundaries.md`: hard import rules and enforcement.
- `engine_api_contract_model_policy.md`: contract classification and ABC/Protocol usage policy for `engine.api.*`.

Update these docs when architecture-level contracts change.
